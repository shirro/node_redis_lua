Patch redis script commands into node-redis prototype. Does not assume node-redis
has been patched with scripting support so can use the standard version.

Requires:
	https://github.com/mranney/node_redis
	coffescript (optional)
	Redis scripting branch, https://github.com/antirez/redis/tree/scripting

Installation. Not in npm yet so clone the repo and use.

    npm install .

To use require lua:

    lua = require('redis-lua').lua

Add some redis scripts:

    lua 'myset', 2, 'return redis.call("set", KEYS[1], KEYS[2])'

And call like a regular redis command:

    r = require('redis').createClient()
    r.myset 'testing', 'surprise', (err, res) ->
    	if (err)
		console.log err
	else
		console.log res
